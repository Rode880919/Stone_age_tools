# sa_pet_assistant 宠物基础知识教程

这份文档不是纯概念表，而是按“先建立直觉，再看公式，再看例子”的顺序来讲。

如果你刚接触这个项目，建议按下面顺序阅读：

1. 先看第 1 节到第 4 节，建立整体概念
2. 再看第 5 节到第 8 节，理解普通宠、转生、融合、喂药分别在算什么
3. 最后看第 9 节的例子，把前面的概念串起来

## 1. 先记住这几个核心词

在 `sa_pet_assistant` 里，下面几个词是最重要的：

- `成长率`：宠物的 `V / S / T / D`
- `VSTD`：四项成长率之和，也就是 `V + S + T + D`
- `蛋基础成长`：融合完成后、喂药前的蛋面 `V / S / T / D`
- `孵化后成长`：按孵化公式算出来的四项成长，不含出生后 10 点随机加点
- `出生后 10 点随机`：宠物出生时额外随机分配的 10 点，不属于成长档本身

把它们粗略理解成：

```text
宠物成长率 (V,S,T,D)
    ↓ 决定长期成长方向
面板属性 (HP, ATK, DEF, SPD)
    ↓ 是你在游戏里看到的结果
```

但是中间还有很多随机和取整步骤，所以不能简单理解成“成长率直接等于面板”。

## 2. V / S / T / D 到底影响什么

工具当前使用的显示公式是：

```text
HP  = floor((V*4 + S + T + D) / 100)
ATK = floor((S + T*0.1 + V*0.1 + D*0.05) / 100)
DEF = floor((T + S*0.1 + V*0.1 + D*0.05) / 100)
SPD = floor(D / 100)
```

你可以先用最直觉的方式记：

- `V` 主要管生命
- `S` 主要管攻击
- `T` 主要管防御
- `D` 主要管敏捷

但四项不是完全隔离的：

- `V` 也会少量影响攻击、防御
- `D` 也会少量影响攻击、防御
- `S` 与 `T` 之间也会互相少量影响

### 一个最简单的理解例子

假设有两只宠：

```text
A: V/S/T/D = 40 / 30 / 30 / 20
B: V/S/T/D = 30 / 40 / 30 / 20
```

直观上：

- A 的生命会更高
- B 的攻击会更高
- 防御和敏捷则比较接近

所以 `V / S / T / D` 更像是“成长方向”，不是单纯的评分数字。

## 3. 什么叫成长率，什么叫实际生效成长率

工具里可以把数据理解成下面 4 层：

```text
基础成长率/成长档 (V,S,T,D)
    ↓ 可能经过 ±2 波动
实际生效成长率 stored(V,S,T,D)
    ↓ 配合 INIT 和随机加点，累计成 raw 点数
原始点数 raw(V,S,T,D)
    ↓ 按显示公式向下取整
面板属性 (HP, ATK, DEF, SPD)
```

这里最容易混淆的是：

- 页面里输入或输出的 `V / S / T / D`，通常是在说“成长率/成长档”这一层
- 但真正参与 1 级初始化和后续升级的，是波动后的 `stored(V,S,T,D)`

也就是说，同一只宠物的基础成长率，即使固定，最终仍然会因为随机而长得不完全一样。

## 4. VSTD 和 Rank 是什么关系

### 普通宠物的 Rank 表

```text
VSTD >= 100 -> Rank 0
VSTD >= 95  -> Rank 1
VSTD >= 90  -> Rank 2
VSTD >= 85  -> Rank 3
VSTD >= 80  -> Rank 4
否则          Rank 5
```

### 转生宠物的 Rank 表

```text
VSTD >= 130 -> Rank 0
VSTD >= 100 -> Rank 1
VSTD >= 95  -> Rank 2
VSTD >= 85  -> Rank 3
VSTD >= 80  -> Rank 4
否则          Rank 5
```

### 这里最关键的一点

Rank 在当前代码口径里，不是拿“理想中的基础成长率”直接判，而是：

- 先经过 `±2` 波动
- 再用波动后的 `VSTD` 判 Rank

所以有时你会看到：

- 同一只基础宠，因为 `±2` 波动不同
- 实际进入成长流程时的 Rank 也可能不同

## 5. 普通宠物的随机过程，到底发生在哪几步

## 5.1 第一步：`±2` 波动

普通宠出生时，四项成长率会独立做一次 `±2` 波动：

```text
ev_X = RAND(0,4) - 2
stored_X = base_X + ev_X
```

这一步的影响很大，因为它会同时影响：

- 实际生效成长率
- `VSTD`
- Rank 判定

### 小例子

如果基础成长率是：

```text
40 / 35 / 30 / 25
```

一次随机后可能变成：

```text
41 / 33 / 31 / 27
```

也可能变成：

```text
38 / 37 / 28 / 24
```

这两组的 `VSTD` 可能一样，也可能不一样；如果跨过 Rank 边界，就会影响后续成长倍率区间。

## 5.2 第二步：出生时 10 点随机分配

普通宠出生时，还会有一组额外 10 点随机分配。

当前工具代码里，这 10 点分配默认带一个配套模拟限制：

- 单项最多 `4`

这条要单独说明：

- 这是当前项目里“宠物成长模拟”的设定
- 不是本文已从服务器源码中直接确认的硬性上限
- 如果按“10 次独立、每次等概率落到 4 维之一”的无上限模型看，某一预先指定属性拿到 `5` 点及以上的概率服从二项分布
- `X ~ Binomial(10, 1/4)`
- `P(X >= 5) = Σ C(10,k) (1/4)^k (3/4)^(10-k) ≈ 7.81%`
- `P(X >= 6) ≈ 1.98%`
- `P(X >= 7) ≈ 0.35%`

所以，“单项超过 4 点”并不是理论上不会发生，只是对某一指定单项而言概率不高。

也就是：

- 总共 10 点
- 分到 `V/S/T/D`
- 但某一项不会拿到超过 4 点

1 级 raw 点数按下面方式形成：

```text
raw_X_lv1 = INITNUM * (stored_X + extra_X)
```

### 小例子

假设：

```text
INITNUM = 20
stored = 40 / 35 / 30 / 25
extra  = 3 / 2 / 4 / 1
```

那么 1 级 raw 点数就是：

```text
V = 20 * (40 + 3)
S = 20 * (35 + 2)
T = 20 * (30 + 4)
D = 20 * (25 + 1)
```

再把这组 raw 点数通过显示公式换算成 1 级面板属性。

## 5.3 第三步：升级时再随机

宠物每升一级，还会再经历两种随机：

1. 再分一次 10 点随机加点
2. 再抽一次 `fRand` 倍率

升级增量是：

```text
delta_X = floor((stored_X + Param_X) * fRand)
```

这里要注意：

- 升级时的 10 点随机分配，没有“单项最多 4”这个限制
- `fRand` 的范围由 Rank 决定

所以最终练到高等级时，同一只宠也会出现一个分布，而不是一个固定值。

## 6. 反推计算页，为什么只能给“候选”

“反推计算”页的输入是：

- 不同等级下的显示属性 `HP / ATK / DEF / SPD`

它要反推出：

- 候选成长率 `V / S / T / D`
- 候选 `INITNUM`

但由于中间有：

- `±2`
- 出生时 10 点随机
- 升级随机
- 显示公式向下取整

所以这件事天然不是“唯一解”。

### 一个直觉例子

你看到一只宠在：

```text
Lv1   -> 一组面板
Lv70  -> 一组面板
Lv140 -> 一组面板
```

工具会去找：

- 哪几组 `V / S / T / D`
- 配上哪些 `INITNUM`
- 最能解释这几组观测值

所以反推页输出的是：

- 候选
- 误差
- 相对置信度

而不是“绝对唯一真值”。

## 7. 转生到底在做什么

转生可以理解成两段：

1. 先根据原宠、MM、等级和转生次数，算出一组新的基础成长率
2. 再让这组新成长率像普通宠一样进入出生和升级流程

也就是说，转生并不是“直接把最终 140 级属性算出来”，而是：

```text
原宠成长率 + MM + 等级 + 转生次数
    ↓
算出转生后的基础成长率 new_alloc
    ↓
再经历 ±2、Rank、出生 10 点随机、升级随机
    ↓
得到后续真正的属性分布
```

## 7.1 转生输入是什么

当前转生页用到的输入是：

- 原宠 `V / S / T / D`
- MM `V / S / T / D`
- 宠物等级
- 转生次数
- `转生能力上限`

其中：

- 首转默认`转生能力上限`是 `150`
- 二转默认`转生能力上限`是 `200`
- 页面允许你手工改这个上限

## 7.2 第一步：先看原宠的 VSTD 和 Rank

转生时，原宠并不是直接拿四项逐项套公式，而是先看总和：

```text
total2 = pet[0] + pet[1] + pet[2] + pet[3]
```

然后根据转生次数，选择不同的 Rank 表：

- 首转：使用普通宠 Rank 表
- 二转：使用转生宠 Rank 表

### 首转时的 Rank 表

```text
VSTD >= 100 -> Rank 0
VSTD >= 95  -> Rank 1
VSTD >= 90  -> Rank 2
VSTD >= 85  -> Rank 3
VSTD >= 80  -> Rank 4
否则          Rank 5
```

### 二转时的 Rank 表

```text
VSTD >= 130 -> Rank 0
VSTD >= 100 -> Rank 1
VSTD >= 95  -> Rank 2
VSTD >= 85  -> Rank 3
VSTD >= 80  -> Rank 4
否则          Rank 5
```

### 这一步的重要性

因为后面的 `Fx` 会直接由这个 rank 决定。

## 7.3 第二步：计算 Fx

当前代码里：

```text
Fx = floor((5 - petrank) * 1.2) + 5
```

把各 rank 代进去，会得到：

```text
Rank 0 -> Fx = 11
Rank 1 -> Fx = 9
Rank 2 -> Fx = 8
Rank 3 -> Fx = 7
Rank 4 -> Fx = 6
Rank 5 -> Fx = 5
```

所以：

- 原宠 rank 越高，`Fx` 越大
- 等级加成项 `floor((LV-100)/Fx)` 通常会更小一些

## 7.4 第三步：计算 MM 总和和等级截断

当前实现会先处理两个中间量：

### MM 总和

```text
total1 = min(sum(MM), 150)
```

也就是说：

- MM 四项总和最多按 `150` 参与计算
- 超过 `150` 的部分不会继续提高转生能力值（内部变量写作 `ans`）

### 等级截断

```text
lv = min(pet_level, 130)
```

也就是说：

- 宠物等级超过 `130` 以后
- 在转生能力值公式里仍只按 `130` 参与

## 7.5 第四步：计算转生能力值

当前公式是：

```text
ans = floor((total1/100)^5 * 1.3) + total2 + floor((lv - 100) / Fx)
```

然后再做`转生能力上限`限制：

```text
ans = min(ans, ans_cap)
```

这里公式里的内部变量名写作 `ans`、`ans_cap`，界面与说明里统一称为“转生能力值”“转生能力上限”。

其中：

- 首转默认转生能力上限是 `150`
- 二转默认转生能力上限是 `200`
- 页面允许覆盖这个默认值

### 这一步怎么理解

可以把转生能力值（内部变量写作 `ans`）看成：

- 原宠总和 `total2`
- 加上 MM 带来的提升
- 再加上高等级带来的额外补正

但最后会被上限卡住。

### 一个简单例子

假设：

```text
原宠 VSTD = 130
MM 总和 = 120
宠物等级 = 131
首转
```

则：

1. 首转按普通表判原宠 rank
2. 算出对应 `Fx`
3. 等级先截成 `130`
4. 用 `120` 去算 `(total1/100)^5 * 1.3`
5. 最后再和 `total2`、等级项相加
6. 再和 `150` 上限比较

所以最终转生能力值不会无限增长，它一定受上限控制。

## 7.6 第五步：把转生能力值分配到四项 new_alloc

转生能力值算出来以后，还不会直接平均分到四项。

当前代码是：

```text
new_alloc[d] = ans * (stone[d] + pet[d] * 4) // (total1 + total2 * 4)
```

这里有两个要点：

1. 原宠权重是 `4`
2. MM 权重是 `1`

所以：

- 原宠四项分布的影响更大
- MM 更多是调整方向，而不是推翻原宠结构

### 一个分配例子

假设：

```text
原宠 = 40 / 35 / 30 / 25
MM   = 10 / 20 / 5 / 15
```

则分配权重不是：

```text
40+10, 35+20, 30+5, 25+15
```

而是：

```text
40*4+10, 35*4+20, 30*4+5, 25*4+15
```

所以最终 `new_alloc` 仍会明显偏向原宠原本的结构。

## 7.7 第六步：转生后还会继续随机

这里是最容易漏掉的一点。

转生公式算出的：

```text
new_alloc
```

只是“转生后的基础成长率”。

真正进入后续成长前，还会继续经历：

1. 四项独立 `±2` 波动
2. 用波动后的 `VSTD` 按转生表判 Rank
3. 出生时 10 点随机分配
4. 后续升级时每级 10 点随机分配
5. 每级再抽 `fRand`

也就是：

```text
new_alloc
    ↓
stored = new_alloc + ±2
    ↓
用 stored 的和判 Rank
    ↓
出生再加 10 点随机
    ↓
得到 1 级 raw 点数
    ↓
之后每级继续随机成长
```

## 7.8 转生后出生时，这几步分别影响什么

### 7.8.1 `±2` 波动

作用：

- 改变真正存入成长档的四项值
- 改变波动后的 `VSTD`
- 改变最终 Rank

### 7.8.2 出生时 10 点随机分配

作用：

- 改变 1 级 raw 点数
- 改变出生时 1 级面板属性
- 不改写已经确定的成长档本身

当前项目的配套成长模拟里，这 10 点分配仍默认有：

- 单项最多 `4`

这里同样要注意：这是模拟口径，不是已经由服务器源码直接确认的硬性限制。

### 7.8.3 后续升级随机

作用：

- 每级继续分 10 点随机加点
- 每级继续抽一次 `fRand`
- 所以同一组 `new_alloc` 最终练成的属性仍然是一个分布，不是一条固定值

## 7.9 一个完整转生例子

假设：

```text
原宠 = 40 / 35 / 30 / 25
MM   = 10 / 20 / 5 / 15
宠物等级 = 131
首转
```

流程是：

1. 先算原宠总和：

```text
total2 = 40 + 35 + 30 + 25 = 130
```

2. 首转按普通表看 `130`，得到原宠 rank
3. 根据 rank 算 `Fx`
4. 再算 MM 总和：

```text
total1 = min(10 + 20 + 5 + 15, 150) = 50
```

5. 等级截成：

```text
lv = min(131, 130) = 130
```

6. 代入转生能力值公式，算出一组 `ans`
7. 再和首转上限 `150` 比较
8. 用原宠权重 `4`、MM 权重 `1` 把这组转生能力值分配成 `new_alloc`
9. 转生后的这组 `new_alloc` 再经历：
   - `±2`
   - 用转生表判 Rank
   - 出生时 10 点随机
   - 后续升级随机

最终你在“转生模拟”页看到的，不是“第 8 步就结束”，而是：

- 第 8 步之后再跑完整出生和成长过程
- 最后得到 140 级时的属性分布

## 7.10 转生章节最容易混淆的几点

### 7.10.1 `new_alloc` 不是最终宠物面板

它只是转生后的基础成长率。

### 7.10.2 首转和二转，原宠 rank 表不一样

- 首转看普通表
- 二转看转生表

### 7.10.3 等级超过 130，不会继续增加转生等级补正

因为公式里先做了：

```text
lv = min(pet_level, 130)
```

### 7.10.4 转生能力值不是无限增长的

因为最后一定会被转生能力上限截断：

- 首转默认 `150`
- 二转默认 `200`
- 页面允许你手动改

## 8. 融合成蛋到底在做什么

融合页现在算的是一个确定性的过程。

输入：

- 主宠：等级 + `V / S / T / D`
- 副宠1：等级 + `V / S / T / D`
- 副宠2：可选

当前实现步骤是：

1. 等级小于 80 的宠，先做 `* 8 // 10`
2. 副宠逐项求和
3. 副宠逐项做整数平均
4. 副宠平均值再做 `* 4 // 10`
5. 主宠修正值做 `* 6 // 10`
6. 两边相加，得到蛋基础成长

### 例子

假设：

```text
主宠   = 80级, 40 / 35 / 30 / 25
副宠1  = 80级, 30 / 30 / 30 / 30
```

则：

```text
主宠贡献   = floor(主宠 * 0.6)
副宠贡献   = floor(副宠平均 * 0.4)
蛋基础成长 = 主宠贡献 + 副宠贡献
```

如果还有副宠2，则副宠部分先平均，再统一乘 `0.4`。

### 8.0.0 低于 80 级时，为什么要先乘 0.8

当前融合代码与 `gmsv` 一致：

- 主宠如果等级 `< 80`，先对四项分别做：`floor(value * 0.8)`
- 每只副宠如果等级 `< 80`，也先对四项分别做：`floor(value * 0.8)`

也就是说，`0.8` 修正发生在：

- 主宠 `0.6` 贡献之前
- 副宠求和、平均、`0.4` 贡献之前

所以正确顺序是：

```text
先做 floor(value * 0.8)
再进入主宠/副宠后续流程
```

不是：

```text
先算 0.6 或 0.4 贡献，最后再统一乘 0.8
```

### 8.0.0 一个低等级取整例子

假设某宠某一项成长是：

```text
39
```

如果它等级 `< 80`，当前正确处理是：

```text
floor(39 * 0.8) = floor(31.2) = 31
```

后面所有主宠贡献或副宠贡献，都是基于这个 `31` 继续往下算。

### 8.0.1 主宠贡献的取整，到底在哪一步发生

主宠部分当前代码与 `gmsv` 一致，顺序是：

1. 如果主宠等级 `< 80`，先做：`floor(value * 0.8)`
2. 再对修正后的主宠逐项做：`floor(value * 0.6)`

也就是：

```text
主宠贡献[i] = floor( 主宠修正后值[i] * 0.6 )
```

如果把低等级修正也写开，就是：

```text
主宠贡献[i] = floor( floor(原值[i] * 0.8) * 0.6 )    （仅当主宠 < 80 级）
```

而不是：

```text
floor(原值[i] * 0.8 * 0.6)
```

虽然有些数值结果会碰巧相同，但源码的真实顺序是分两步取整。

### 8.0.1 一个主宠取整例子

假设主宠某一项原值是：

```text
39
```

并且主宠等级 `< 80`，则：

```text
先修正：floor(39 * 0.8) = 31
再取主宠贡献：floor(31 * 0.6) = floor(18.6) = 18
```

所以最终主宠贡献是：

```text
18
```

### 8.0.2 副宠平均值的取整，到底在哪一步发生

这是融合里最容易算错的一步。当前代码与 `gmsv` 一致，副宠部分不是“先乘 0.4 再平均”，而是严格按下面顺序：

1. 每只副宠先各自做等级修正
   如果等级 `< 80`，则每项先做：`floor(value * 0.8)`
2. 把所有副宠修正后的四项逐项相加
3. 再逐项做整数平均：`floor(sum / count)`
4. 最后对这个平均值逐项做：`floor(avg * 0.4)`

也就是：

```text
副宠贡献[i] = floor( floor(副宠修正后总和[i] / 副宠数量) * 0.4 )
```

不是：

```text
floor(副宠修正后总和[i] * 0.4 / 副宠数量)
```

也不是：

```text
floor(每只副宠先乘 0.4 再求平均)
```

### 8.0.2 一个取整例子

假设两只副宠在某一项修正后分别是：

```text
31 和 30
```

那么当前正确顺序是：

```text
先求和：31 + 30 = 61
再平均：floor(61 / 2) = 30
再乘 0.4：floor(30 * 0.4) = 12
```

结果是：

```text
副宠贡献 = 12
```

如果你把步骤错误合并成：

```text
floor(61 * 0.4 / 2) = floor(12.2) = 12
```

这个例子刚好还是一样；但不是所有情况都一样，所以文档和代码都坚持按源码步骤逐步取整，不做公式合并。

## 8.1 融合页当前没有算什么

当前融合页明确不包含：

- 融合后蛋面的 `±2` 随机
- 出生后的 10 点随机加点

因此这个页面更像是：

- 看融合过程
- 看一个确定性的蛋基础成长基准

### 8.2 从融合到真正出生，中间还会再发生什么

如果继续按 `gmsv` 的真实流程往后走，融合得到的 `work[0..3]` 在进入 `PETFUSION_SetNewEgg()` 之后，还会继续经历两步：

1. 四项各自再做一次 `±2` 波动
2. 出生时再做 10 次随机 `+1`

也就是：

```text
work[0] += RAND(0,4) - 2
work[1] += RAND(0,4) - 2
work[2] += RAND(0,4) - 2
work[3] += RAND(0,4) - 2
```

然后：

```text
10 次随机选择 V/S/T/D 其中一项，各 +1
```

### 8.2.1 这两步分别影响什么

这两步的作用并不一样：

- `±2` 波动会影响真正存入 `CHAR_ALLOCPOINT` 的成长率
- 波动后的四项和会决定融合宠出生时的 Rank
- 后面的 10 点随机会影响出生时 1 级的初始 raw 点数和面板属性
- 但这 10 点随机不会改写已经存入的成长档本身

可以把它理解成：

```text
融合确定性结果
    ↓
±2 波动后的真实成长档
    ↓
根据这组成长档判 Rank
    ↓
出生再加 10 点随机
    ↓
得到 1 级面板属性
```

### 8.2.2 为什么当前“融合成蛋”页没有把这两步算进去

因为当前页面的定位是：

- 展示融合公式本身
- 展示确定性的蛋基础成长
- 展示无喂药时的孵化后成长基准

如果把 `±2` 和出生后 10 点随机也一起算进去：

- 页面输出就会从“确定性计算”变成“随机结果”
- 同一组输入会出现很多可能结果
- 它就更接近“出生模拟”，而不是“融合公式查看器”了

所以当前页面明确停在：

- 蛋基础成长
- 孵化后成长（不含出生后 10 点随机）

### 8.2.3 一个完整的小流程例子

假设融合后的确定性结果是：

```text
蛋基础成长 = 36 / 33 / 28 / 25
```

如果不喂药，先按孵化公式算出：

```text
孵化后成长 = 38 / 35 / 30 / 27
```

接着如果真的进入出生流程，还会继续发生：

1. `±2` 波动，可能变成：

```text
39 / 33 / 31 / 28
```

2. 用这组波动后的四项求 `VSTD`，并判定 Rank
3. 再做出生时 10 点随机分配，例如：

```text
2 / 3 / 4 / 1
```

4. 最终才形成 1 级 raw 点数和你看到的出生面板

所以一定要分清：

- 融合结果
- 孵化后成长
- 真正出生后的 1 级状态

这三层不是同一个东西。

## 9. 蛋喂药和孵化，到底在优化什么

“融合蛋喂药”页输入的是：

- 蛋基础成长
- 喂药方案

注意，这里输入的不是宠物最终成长率，而是蛋的基础面板。

## 9.1 默认药值

当前默认 5 个等级药的原始值是：

```text
Lv1 = 25
Lv2 = 50
Lv3 = 75
Lv4 = 100
Lv5 = 125
```

而且当前页面固定要求：

```text
必须喂满 40 颗
```

## 9.2 药不是直接加到该项上

这是最重要的一点。

喂药后并不是“某项吃了多少，就直接加到某项成长上”。

当前流程是：

1. 每项先累加药原始值
2. 再折算成 `work[i] = floor(raw[i] * 7 / 1000)`
3. 单项最多 `60`
4. 如果折算总和 `> 50`，先整体压缩到 `50`
5. 再和蛋基础成长一起重新分配
6. 最后再做单项 `60` 限制和总和 `150` 限制

### 这意味着什么

意味着下面两种情况未必一样：

```text
情况 A：全堆在 V
情况 B：部分分给其他项，再通过重新分配拉回 V
```

这也是为什么“融合蛋喂药”页需要做搜索，而不是只写一个简单加法器。

## 9.3 当前页面优化的目标是哪一层

当前页面优化的是：

- `孵化后成长`
- 不含出生后的 10 点随机加点

所以不要把它误会成：

- 优化蛋基础成长
- 或者直接优化出生后的最终 1 级面板

## 10. 一个完整例子：从成长率到高等级面板

假设一只普通宠的基础成长率是：

```text
40 / 35 / 30 / 25
INITNUM = 20
```

它实际会经历：

1. `±2` 波动
   例如变成：`41 / 33 / 31 / 27`
2. 用这组波动后的四项求 `VSTD`
3. 判定 Rank
4. 出生时再分 10 点
   例如：`3 / 2 / 4 / 1`
5. 得到 1 级 raw 点数
6. 再换算成 1 级显示属性
7. 以后每次升级再做：
   - 一次 10 点随机分配
   - 一次 `fRand` 抽取
8. 最终形成 140 级时的一整组属性分布

所以同样写成“40/35/30/25”，最终显示出来的宠物并不是永远同一只。

## 11. 一个完整例子：从融合蛋到喂药优化

假设你先在“融合成蛋”页算出：

```text
蛋基础成长 = 36 / 33 / 28 / 25
```

接着你把它带到“融合蛋喂药”页。

这时页面会做的事不是：

```text
蛋基础成长 + 药效 = 最终结果
```

而是：

1. 先把 40 颗药折算成四项 `work`
2. 先检查是否触发 `50` 压缩
3. 再把 `work` 和 `蛋基础成长` 一起按比例重新分配
4. 最后检查单项 `60` 和总和 `150`
5. 输出 `孵化后成长`

这就是为什么：

- 两个喂法即使总药量相同
- 最终孵化后的主项也可能不同

## 12. 你在用工具时，可以怎么理解每一页

### 反推计算

把它理解成：

- 根据面板属性，反猜最可能的成长率和 `INITNUM`

### 成长模拟

把它理解成：

- 已知成长率后，看长期练成的属性分布

### 转生模拟

把它理解成：

- 先算转生后的基础成长率，再看后续成长分布

### 融合成蛋

把它理解成：

- 先求一个确定性的蛋基础成长基准

### 融合蛋喂药

把它理解成：

- 以蛋基础成长为输入，优化“孵化后成长”

## 13. 最后记住 4 句话

1. `V / S / T / D` 决定成长方向，不直接等于最终面板。
2. `VSTD` 会影响 Rank，但 Rank 是在波动后判的。
3. 蛋基础成长不等于孵化后成长。
4. 孵化后成长也不等于出生后最终宠物面板，因为出生后还有 10 点随机。
