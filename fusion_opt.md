# 融合蛋喂药页面的两种计算方案

本文说明 `sa_pet_assistant` 中“融合蛋喂药”页面目前支持的两种计算方案。

## 0. 术语约定

本文统一使用下列术语：

- `蛋基础成长`：融合后、喂药前的蛋面 `V / S / T / D`
- `孵化后成长`：按 `PET_getEvolutionAns()` 计算后的四项成长结果
- `孵化后成长` 不包含出生后的 10 点随机加点
- 本文中的优化目标，默认都指 `孵化后成长`

## 1. 计算方式1：线性目标

线性目标用于把四项成长按一个统一公式折算成单个分数，再按这个分数寻找最优喂法。

计算公式为：

```text
目标值 = xV * V + xS * S + xT * T + xD * D
```

其中：

- `V / S / T / D` 是孵化后成长，即蛋按 `PET_getEvolutionAns()` 计算后的四项结果（不含出生后 10 点随机）
- `xV / xS / xT / xD` 是页面上输入的四个权重

适用场景：

- 你希望多项属性一起优化
- 你已经有明确的线性评价公式
- 你希望候选解按综合收益排序

特点：

- 会扫描所有可达喂法状态
- 候选解按线性目标值从高到低排序
- 若目标值完全相同，则继续按程序内部的稳定规则排序
- 稳定规则会继续偏向更高总和、以及更高等级素材，尤其优先 `Lv5`

例子：

```text
xV = 1
xS = 2
xT = 0
xD = 1
```

则程序实际优化的是：

```text
V + 2*S + D
```

## 2. 计算方式2：主目标 + 次目标

主目标 + 次目标用于字典序优化，而不是线性加权。

它的含义是：

1. 先让主目标属性尽可能大
2. 若有多个方案主目标相同，再让次目标尽可能大
3. 若仍然相同，再按程序内部稳定规则继续排序

例如选择：

```text
主目标 = S
次目标 = D
```

则程序的比较顺序是：

```text
先比较 S
S 相同再比较 D
再相同则比较总和、材料等级偏好等
```

适用场景：

- 你最关心某一个单项
- 你不希望其他属性通过线性加权“补偿”主属性
- 你只想在主属性并列时，再看第二优先属性

这和线性目标不同。

例如下面两个结果：

```text
A = 40 / 46 / 30 / 35
B = 39 / 48 / 30 / 35
```

如果你的目标是“先看 V，再看 S”：

- 主目标 + 次目标会选 `A`
- 线性目标则可能因为权重设置不同而选 `B`

## 3. 两种方案的区别

可以把两者理解成：

- 线性目标：所有属性一起折算成一个分数
- 主目标 + 次目标：先保第一优先级，再保第二优先级

更直观地说：

- 线性目标允许“用别的属性补回来”
- 主目标 + 次目标不允许低主属性被别的属性补偿

## 4. 页面中的候选解展示

当前页面中，两种计算方式是互斥的：

- 选择“计算方式1：线性目标”时，使用 `xV / xS / xT / xD`
- 选择“计算方式2：主目标 + 次目标”时，使用 `主目标 / 次目标`

候选解表只保留一张，统一展示最终结果。

其中：

- 普通候选解来自全局搜索结果
- `自定义` 行来自上方 `4 x 5` 自定义喂法

## 5. 自定义喂法的行为

页面上方可手工输入 `4 x 5` 喂法：

- 4 行对应 `V / S / T / D`
- 5 列对应 `Lv1 ~ Lv5`

规则：

- 必须总共喂满 `40` 颗
- 输入合法且总数为 `40` 时，会在候选解表中显示一行 `自定义`
- 修改自定义喂法后，`自定义` 行会自动刷新
- `自定义` 行会固定高亮，方便和普通候选解区分

## 6. 当前程序的计算口径

这两种方案只影响“如何排序与挑选候选解”，不改变底层成长计算规则。

底层仍按当前 `gmsv` 已核对的整数流程执行：

1. 蛋药原始值先累加
2. 按 `floor(raw * 7 / 1000)` 折算
3. 先做折算总和压缩
4. 再并入蛋成长
5. 最后做孵化后总和压缩

因此：

- 两种方案的区别在于“优化目标”
- 不在于“孵化公式”

## 7. 选用建议

可按下面方式理解：

- 如果你有明确评分公式，用“线性目标”
- 如果你是“先保某一项，再看另一项”，用“主目标 + 次目标”

常见示例：

- 想尽量冲 `S`，但 `S` 相同时优先 `D`
  - 选择“主目标 + 次目标”
  - `主目标 = S`
  - `次目标 = D`

- 想按自己的综合收益公式排序
  - 选择“线性目标”
  - 例如输入 `xV=1, xS=2, xT=0, xD=1`


## 8. 完整孵化公式

上文第 6 节中的“再并入蛋成长”，在源码里不是简单相加，而是按当前四项基础值重新分配。

按当前 `gmsv` 的 `PET_getEvolutionAns()`，完整顺序是：

1. 蛋药原始值先累加到 `EVOLUTIONBASE`
2. 每项折算：`work[i] = floor(raw[i] * 7 / 1000)`
3. 每项上限 `60`
4. 若四项折算和大于 `50`，则做一次总和压缩
5. 取当前蛋基础成长 `base[i]`
6. 令：

```text
total1 = work[0] + work[1] + work[2] + work[3]
total2 = base[0] + base[1] + base[2] + base[3]
total  = floor(total1 / 2) + total2
```

7. 对每一项分别计算：

```text
fixwork[i] = base[i] + floor(work[i] / 2)
base[i] += floor((fixwork[i] / total) * total1)
```

8. 单项再限制到 `1 ~ 60`
9. 若四项总和大于 `150`，再整体压缩到 `150`

也就是说，“喂药折算得到的收益”不是直接只加在某一项上，而是会参考四项当前基础值重新分配。

因此：

- 同样的其余三项总和，不同分布也可能影响主项最后能否到 `60`
- 优化目标如果关心最终宠物成长，应以“孵化后、未做 10 点随机加点”的四项成长为准
- 不应只盯着蛋面板上的原始 `V / S / T / D`
