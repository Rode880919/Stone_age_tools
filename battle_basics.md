# sa_pet_assistant 战斗基础知识与公式

这份文档面向游戏玩家，但口径尽量严格对齐当前 `gmsv` 源码。

和上一版不同，这一版把能明确写成公式的部分都直接列出来，包括：

- 乱敏与行动顺序
- 骑宠状态下的敏捷参照
- 闪避、会心、反击
- 法术、精灵命中
- `ABIO` 特殊敌方标记
- 属性场地、属性结界、三属性抗性
- 三个职业的战斗技能速度档对照

## 1. 先记一个总原则

当前源码里，战斗判定最容易搞混的是“敏捷到底看哪一层”。

可以先把它分成两层：

- `固定敏`
  更接近角色本回合结算后的真实敏捷。
  装备、套装、部分状态，会先改这里。
- `战斗速度敏`
  更接近“这回合拿来排行动顺序的敏捷”。
  它通常从固定敏出发，再叠加本回合速度类效果。

对玩家的直接结论是：

- 行动顺序主要看 `战斗速度敏`
- 普攻闪避、会心、反击主要看 `固定敏`

## 1A. 常见变量与状态速查

这一节只做“玩家口径翻译”，方便后面看公式时不混。

### 1A.1 `CHAR_WORKFIXDEX`

- 可以把它理解成“这回合用于命中、回避、会心、反击判定的固定敏”。
- 它和面板敏捷不是一回事，但在战斗里，很多概率公式直接读它。
- 当前源码里，`WORKFIXDEX` 主要再经过 `BATTLE_TurnParam()`，叠上速度倍率后，生成 `WORKQUICK`。

### 1A.2 `CHAR_WORKQUICK`

- 可以把它理解成“这回合真正拿来排行动顺序的速度敏”。
- 它通常来自：

```text
WORKQUICK = 由 WORKFIXDEX 经过速度修正后的结果
```

- 所以：
  `WORKFIXDEX` 更偏“概率判定敏”
  `WORKQUICK` 更偏“行动顺序敏”

### 1A.3 `CHAR_LUCK` / `CHAR_WORKFIXLUCK`

- `CHAR_LUCK` 更接近角色原始幸运。
- 玩家每天会被重抽到 `1~5`。
- `CHAR_WORKFIXLUCK` 是战斗里真正拿来判定的幸运，包含装备修正后的结果。
- 对玩家，装备修正后的幸运仍会被压在 `1~5`。
- 对宠物，主 PVP 里通常可以近似按 `0` 看。

### 1A.4 `BATTLE_COM_GUARD`

- 就是“防御 / Guard”动作。
- 守方当前动作是它时，普通闪避函数最前面就直接返回失败，不再走普通回避公式。

### 1A.5 `BATTLE_COM_JYUJYUTU`

- 不是防御。
- 它对应“施法 / 使用法术道具 / 同类直接魔法动作”。
- 守方当前动作是它时，普通闪避公式里的参数从 `0.02` 改成 `0.027`，也就是施法动作的闪避口径和普通站桩不同。

### 1A.6 `CHAR_BATTLEFLG_NODUCK`

- 字面就可以理解成“不能闪避”。
- 只要身上挂着这个战斗旗标，普通闪避直接失败。

### 1A.7 `CHAR_BATTLEFLG_ABIO`

- 这是一个特殊敌方战斗标记。
- 只要挂着它，普通闪避直接失败，且还会影响反击、死亡收尾、逃跑难度等逻辑。

### 1A.8 `CHAR_WORKBATTLECOM3`

- 这不是单一含义变量。
- 它更像“当前战斗动作的附加参数槽”。
- 高 16 位和低 16 位会被不同动作复用，必须结合当前 `BATTLE_COM_*` 一起解读。

常见读法：

- 职业技能里，经常是：

```text
高16位 = 技能等级
低16位 = 阵列 / 子类型 / 附加参数
```

- 法术 / 法术道具里，经常是：

```text
低16位 = magic id 或道具参数
高16位 = magic level 或 0
```

- 在普通闪避里，如果守方当前动作是 `BATTLE_COM_S_NOGUARD`，源码会直接读取：

```text
高16位 CHAR_WORKBATTLECOM3
```

把它当成额外闪避修正值。


## 2. 源码里的取整规则

这一条非常重要。

### 2.1 随机宏

源码使用：

```c
#define RAND(x,y) ((x-1)+1+(int)((double)(y-(x-1))*rand()/(RAND_MAX+1.0)))
```

可以把它理解成：

- 在某个区间内取一个整数随机值
- 最终仍然会落到整数

### 2.2 大多数百分比乘法，都会因为 `int` 赋值而直接截断

例如：

```c
At_Dex *= 0.8;
Df_Dex *= 0.6;
dex = old_dex * 0.9;
```

因为目标变量是 `int`，所以结果会直接截断，不会四舍五入。

本文后面看到的：

- `*0.8`
- `*0.6`
- `*0.9`
- `sqrt(...)`

只要最后写回整数，都要按“截断”理解。

## 3. 乱敏机制与行动顺序

### 3.1 普通行动的基础公式

当前源码里，大多数普通行动的顺序值是：

```text
work = 参照敏捷 + 20
顺序值 = work - RAND(0, work * 0.3)
```

所以：

- 先加 `20`
- 再扣掉 `0 ~ 30%` 的整数随机值

这就是玩家常说的“乱敏”。

### 3.2 使用道具

```text
work = 参照敏捷 + 20
顺序值 = work - RAND(0, work * 0.3) + work * 0.15
```

也就是在普通乱敏基础上，再额外加 `15%`。

### 3.3 迅速攻击类

```text
work = 参照敏捷 + 20
顺序值 = work + work * 0.3
```

当前源码里是直接加 `30%`，不是随机。

## 4. 骑宠状态下，行动顺序到底看谁的敏

这一节只说当前看到的编译口径：

- `_BACK_VERSION` 未开启
- `_BATTLE_NEWPOWER` 已开启

### 4.1 先分两种情况

#### 情况 A：这个行动走“默认速度档”

这时如果没骑宠：

```text
参照敏捷 = 人物 WORKQUICK
```

如果有骑宠：

- 装备远程武器时：

```text
参照敏捷 = 人物 WORKQUICK * 0.8 + 骑宠 WORKQUICK * 0.2
```

- 装备近战武器或空手时：

```text
参照敏捷 = 人物 WORKQUICK * 0.2 + 骑宠 WORKQUICK * 0.8
```

然后再套：

```text
work = 参照敏捷 + 20
顺序值 = work - RAND(0, work * 0.3)
```

#### 情况 B：这个行动走“职业技能专用速度档”

这类技能在 `BATTLE_DexCalc()` 里直接写死为：

```text
work = 人物 WORKQUICK + 20
```

也就是说：

- 不吃骑宠敏捷混合
- 也不区分你拿的是远程还是近战
- 直接按人物自己的 `WORKQUICK` 算

### 4.2 什么算远程武器

当前源码里，`BATTLE_IsThrowWepon()` 把下面这些都算远程：

- `ITEM_BOW`
- `ITEM_BOOMERANG`
- `ITEM_BREAKTHROW`
- `ITEM_BOUNDTHROW`

也就是：

- 弓
- 回力类
- 投掷类

都走“远程武器”分支。

### 4.3 一个容易误判的点

不是所有职业技能都用骑宠混合敏。

- 法师里一大批技能，有自己独立速度档，只看人物 `WORKQUICK`
- 白狼、猎人大多数主动战斗技能，走默认档，所以骑宠、远程武器都会影响行动顺序

## 5. 三职业技能速度档对照表

说明：

- “默认档”指：

```text
work = 参照敏捷 + 20
顺序值 = work - RAND(0, work * 0.3)
```

- “人物敏”指直接看人物 `WORKQUICK`
- “骑宠混合敏”指按第 4 节的骑宠公式取参照敏捷
- 这里只列战斗内会进入 `BATTLE_DexCalc()` 或进入战斗流程的职业技能
- 像 `追寻敌踪`、`回避战斗`、`气力充沛`、`负重增加`、`自我强化` 这类不走正常战斗速度判定的，不放进表里

### 5.1 法师系

| 技能 | 速度公式 | 参照敏捷 |
| --- | --- | --- |
| `火山泉` | `work - RAND(0, work*0.2)` | 人物敏 |
| `召雷术` | `work - RAND(0, work*0.2)` | 人物敏 |
| `冰箭术` | `work - RAND(0, work*0.2)` | 人物敏 |
| `嗜血成性` | `work - RAND(0, work*0.3)` | 人物敏 |
| `嗜血蛊` | `work - RAND(0, work*0.3)` | 人物敏 |
| `一针见血` | `work - RAND(0, work*0.3)` | 人物敏 |
| `火附体` | `work - RAND(work*0.3, work*0.5)` | 人物敏 |
| `冰附体` | `work - RAND(work*0.3, work*0.5)` | 人物敏 |
| `雷附体` | `work - RAND(work*0.3, work*0.5)` | 人物敏 |
| `火星球` | `work - RAND(0, work*0.5)` | 人物敏 |
| `电流术` | `work - RAND(0, work*0.5)` | 人物敏 |
| `冰爆术` | `work - RAND(0, work*0.5)` | 人物敏 |
| `火龙枪` | `work - RAND(work*0.2, work*0.5)` | 人物敏 |
| `暴风雨` | `work - RAND(work*0.2, work*0.5)` | 人物敏 |
| `冰镜术` | `work - RAND(work*0.2, work*0.5)` | 人物敏 |
| `附身术` | `work - RAND(work*0.2, work*0.5)` | 人物敏 |
| `移形换位` | `work - RAND(work*0.2, work*0.5)` | 人物敏 |
| `世界末日` | 源码字面是 `work - RAND(0.3, work*0.6)` | 人物敏 |

补充：

- `世界末日` 和 `火龙枪` 在当前 `_PROFESSION_ADDSKILL` 分支下是延迟释放技能。
- `世界末日` 会先进入 `DOOMTIME=3` 的待机状态。
- `火龙枪` 会先进入 `DOOMTIME=2` 的待机状态。
- 真正出手的回合，才按上表的速度档计算顺序。

### 5.2 白狼系

当前源码里，白狼系主动战斗技能大多走“默认档”。

| 技能 | 速度公式 | 参照敏捷 |
| --- | --- | --- |
| `爆击` | 默认档 | 骑宠混合敏 |
| `连环攻击` | 默认档 | 骑宠混合敏 |
| `回避` | 默认档 | 骑宠混合敏 |
| `补血` | 默认档 | 骑宠混合敏 |
| `状态回复` | 默认档 | 骑宠混合敏 |
| `双重攻击` | 默认档 | 骑宠混合敏 |
| `舍已为友` | 默认档 | 骑宠混合敏 |
| `激化攻击` | 默认档 | 骑宠混合敏 |
| `能量聚集` | 默认档 | 骑宠混合敏 |
| `专注战斗` | 默认档 | 骑宠混合敏 |
| `盾击` | 默认档 | 骑宠混合敏 |
| `格档` | 默认档 | 骑宠混合敏 |
| `贯穿攻击` | 默认档 | 骑宠混合敏 |
| `座骑攻击` | 默认档 | 骑宠混合敏 |
| `濒死攻击` | 默认档 | 骑宠混合敏 |
| `回旋攻击` | 默认档 | 骑宠混合敏 |
| `混乱攻击` | 默认档 | 骑宠混合敏 |

补充：

- 白狼技能虽然有的属于“攻击技能”、有的属于“辅助技能”，但当前速度判定大多都没在 `BATTLE_DexCalc()` 单独开档。
- 所以只要骑宠，就会吃到“近战偏宠、远程偏人”的混合敏公式。

### 5.3 猎人系

当前源码里，猎人系主动战斗技能也大多走“默认档”。

| 技能 | 速度公式 | 参照敏捷 |
| --- | --- | --- |
| `陷阱` | 默认档 | 骑宠混合敏 |
| `驯伏宠物` | 默认档 | 骑宠混合敏 |
| `激怒宠物` | 默认档 | 骑宠混合敏 |
| `天罗地网` | 默认档 | 骑宠混合敏 |
| `树根缠绕` | 默认档 | 骑宠混合敏 |
| `体掠夺` | 默认档 | 骑宠混合敏 |
| `毒素武器` | 默认档 | 骑宠混合敏 |
| `火抗性` | 默认档 | 骑宠混合敏 |
| `冰抗性` | 默认档 | 骑宠混合敏 |
| `雷抗性` | 默认档 | 骑宠混合敏 |
| `自然威能` | 默认档 | 骑宠混合敏 |
| `号召自然` | 默认档 | 骑宠混合敏 |
| `四属性结界` | 默认档 | 骑宠混合敏 |
| `团体火抗性` | 默认档 | 骑宠混合敏 |
| `团体冰抗性` | 默认档 | 骑宠混合敏 |
| `团体雷抗性` | 默认档 | 骑宠混合敏 |
| `弱点攻击` | 默认档 | 骑宠混合敏 |
| `挑拨` | 默认档 | 骑宠混合敏 |
| `遗忘` | 默认档 | 骑宠混合敏 |

## 6. 普攻回避与命中

当前源码入口：`BATTLE_DuckCheck()`。

这一节先记两个结论：

- 玩家的幸运有效范围是 `1~5`
- 在你现在关心的主 PVP 概率里，宠物通常不吃幸运修正，所以分析宠物时可近似按 `luck = 0` 看

### 6.1 先看硬性不能回避的情况

守方有下面这些情况时，直接不能走普通回避：

- 当前是 `BATTLE_COM_GUARD`
- 当前有 `吸收 / 反射 / 消失` 这一类伤害反应
- 当前不能行动，且不满足延迟技能那段例外分支
- 身上有 `CHAR_BATTLEFLG_NODUCK`
- 身上有 `ABIO` 标记

这几条命中任何一条，都会直接 `return FALSE`。

### 6.2 `_PETSKILL_SETDUCK` 与独立前判回避

在普通回避公式之前，源码会先查一次：

```text
if BATTLE_CheckMySkillDuck(defindex) == TRUE:
    直接闪避成功
```

这层前判对应的不是职业技能 `回避`，而是 `_PETSKILL_SETDUCK` 这套宠技状态。它实际使用的是两格工作变量：

- `CHAR_MYSKILLDUCK`：剩余持续回合数；大于 `0` 才会进入这层前判
- `CHAR_MYSKILLDUCKPOWER`：这层前判的成功率，按百分数存放

`BATTLE_CheckMySkillDuck()` 的判法很直接：

```text
若 rand()%100 > CHAR_MYSKILLDUCKPOWER:
    前判失败
否则:
    前判成功，直接闪避
```

也就是说，这一层不是“把普通回避率再乘高一点”，而是：

- 先独立判一次固定概率
- 这次判过了，直接闪
- 这次没判过，才继续走后面的普通回避公式

当前这段独立概率，按技能生效后的等级分档：

```text
>=10 : 70%
>=9  : 60%
>=8  : 50%
>=6  : 45%
>=5  : 30%
>=3  : 25%
否则 : 10%
```

持续回合：

```text
>=10 : 5 回合
>=6  : 4 回合
否则 : 3 回合
```

所以如果：

- 独立前判成功率 = `p_front`
- 普通回避公式成功率 = `p_normal`

那么总闪避率是：

```text
total = p_front + (1 - p_front) * p_normal
```

### 6.2.1 `CHAR_WORK_P_DUCK` / `CHAR_WORKMOD_P_DUCK` 是什么

这两个变量才对应职业技能 `回避`：

- `CHAR_WORK_P_DUCK`：本回合是否启用职业 `回避` 的标记，通常写成 `1`
- `CHAR_WORKMOD_P_DUCK`：职业 `回避` 的倍率值

它们不是独立前判，而是在普通回避值已经算出来、并且 `AddHit` 已经扣完之后，再做一次倍率放大：

```text
per = per * (100 + CHAR_WORKMOD_P_DUCK) / 100
```

所以源码里这两套“回避”机制要分开理解：

- `_PETSKILL_SETDUCK / CHAR_MYSKILLDUCK*`：独立前判，判过就直接闪
- `CHAR_WORK_P_DUCK / CHAR_WORKMOD_P_DUCK`：后段倍率，把普通回避率再放大

### 6.3 普通回避公式本体

先取：

```text
At_Dex  = 攻方 CHAR_WORKFIXDEX
Df_Dex  = 守方 CHAR_WORKFIXDEX
Df_Luck = 守方 CHAR_WORKFIXLUCK
```

但这里要注意：

- 只有守方是玩家时，源码才真的把 `Df_Luck` 加进回避公式
- 守方是宠物时，这一项在主 PVP 口径里等效为 `0`

身份修正：

- 敌打宠：`At_Dex = floor(At_Dex * 0.8)`
- 非敌打宠：`Df_Dex = floor(Df_Dex * 0.8)`
- 非玩家打玩家：`At_Dex = floor(At_Dex * 0.6)`
- 玩家打非玩家：`Df_Dex = floor(Df_Dex * 0.6)`

接着：

```text
若 Df_Dex >= At_Dex：
  Big = Df_Dex
  Small = At_Dex
  wari = 1.0
否则：
  Big = At_Dex
  Small = Df_Dex
  wari = Small / Big
```

然后看守方当前动作：

- 如果守方当前动作是 `BATTLE_COM_JYUJYUTU`：`gKawashiPara = 0.027`
- 否则：`gKawashiPara = 0.02`

这里的 `BATTLE_COM_JYUJYUTU` 不是防御，而是“施法 / 使用法术道具”的那类动作。

普通回避核心值：

```text
Work = (Big - Small) / gKawashiPara
if Work <= 0 then Work = 0
per = sqrt(Work) * wari
per += Df_Luck
```

### 6.4 源码里的精确修正顺序

在上面的 `per` 之后，源码继续做：

```text
per += gBattleDuckModyfy
攻方醉酒时：per += RAND(20, 30)
攻方拿弓时：per += 20
守方当前是 BATTLE_COM_S_NOGUARD：per += 高16位 CHAR_WORKBATTLECOM3
攻方拿弓时：per += 20
```

注意：

- 弓的 `+20` 在源码里被加了两次
- 也就是实际是 `+40`

然后才会：

```text
per *= 100
per 上限 = 7500
per 下限 = 1
```

也就是普通回避公式先被压到：

```text
0.01% ~ 75.00%
```

之后才继续做：

- 如果开了 `_EQUIT_HITRIGHT`，且攻方是玩家：

```text
per -= RAND(AddHit*0.8, AddHit*1.2)
per 不能低于 0
```

- 如果守方身上存在职业闪避倍率状态：

```text
per = per * (100 + CHAR_WORKMOD_P_DUCK) / 100
```

- 如果攻方当前动作是 `混乱攻击`：

```text
per += per * 0.4
```

最后才判：

```text
若 RAND(1, 10000) <= per，则闪避成功
```

### 6.5 玩家 PVP 最终公式速查

下面这组是最常用的“玩家对玩家、忽略特殊修正”的最终公式。

设：

- `x = 攻方固定敏`
- `y = 守方固定敏`
- `L = 守方幸运`

且只考虑：

- 不开主动 `回避`
- 不考虑弓
- 不考虑醉酒
- 不考虑 `AddHit`
- 不考虑职业闪避倍率
- 不考虑 `混乱攻击`
- 不考虑施法动作修正

则普通回避率：

```text
若 y >= x：
  dodge_base(x, y) = sqrt((y - x) / 0.02)
否则：
  dodge_base(x, y) = sqrt((x - y) / 0.02) * (y / x)
```

最终普通回避率：

```text
dodge(x, y, L) = clip(dodge_base(x, y) + L, 0.01, 75.00)
```

最终普通命中率：

```text
hit(x, y, L) = 100.00 - dodge(x, y, L)
```

其中：

- 玩家 `L` 的有效范围只有 `1~5`
- 宠物在这套主 PVP 概率里通常可近似按 `L = 0`

### 6.6 一个现实口径样例

假设：

```text
攻方固定敏 = 430
守方固定敏 = 400
守方幸运   = 3
```

因为守方敏低于攻方：

```text
Big = 430
Small = 400
wari = 400 / 430 = 0.9302...
Work = (430 - 400) / 0.02 = 1500
sqrt(Work) = 38.73...
per = 38.73 * 0.9302 + 3 = 39.03%
```

也就是：

- 守方普通闪避率约 `39.03%`
- 攻方普通命中率约 `60.97%`

如果攻方还有 `AddHit = 800` 的命中修正，则这一段会在后面直接再扣约 `8%`，守方闪避会降到约 `31.03%`。

### 6.7 敏捷对敏捷热力图，无 luck 影响

这张图只看：

- 玩家对玩家
- 横轴、纵轴都用 `CHAR_WORKFIXDEX`
- 守方幸运固定 `0`
- 不考虑主动 `回避`
- 不考虑弓、醉酒、`AddHit`、职业修正

实际玩家图形可在此基础上整体上移 `1~5%`。

![回避率基准图](battle_heatmaps/dodge_pvp_luck0.svg)

### 6.8 命中 / 回避的控制策略

如果你的目标是“提高物理命中”：

- 第一优先级仍然是把自己的 `CHAR_WORKFIXDEX` 拉到不低于目标。
- 对已经接近 `75%` 闪避上限的目标，`AddHit` 比继续堆敏更稳定，因为它发生在上限之后。
- 打高闪目标时，尽量避免弓。当前源码里弓会给目标平白加 `40` 点闪避率基础值，再去吃 75% 上限。
- 对面如果正在用主动 `回避`，最大的提升手段往往不是多追那几点幸运，而是直接把命中修正或敏差拉上去，因为主动 `回避` 是额外前判。

如果你的目标是“提高物理回避”：

- 当你的固定敏已经不低于对面时，敏差收益非常高，但也很快碰到 75% 上限。
- 玩家幸运只有 `1~5`，所以它确实有用，但更像“最后补几档”，不是主导变量。
- 主动 `回避` 是最强的单次跳变项，因为它是额外前判，不是简单乘法。

### 6.9 敏捷对命中是否一阶导非负

如果只看玩家 PVP 基准式：

```text
hit(x, y, L) = 100 - dodge(x, y, L)
```

并且先忽略上限、下限裁切，只看基础体，那么答案是：

- 不是
- 对攻方自己的固定敏 `x` 来说，命中率的一阶导不是全局非负

分段写开：

当 `x < y` 时：

```text
∂hit/∂x = 1 / (2 * sqrt(0.02) * sqrt(y - x)) > 0
```

也就是：

- 攻方敏低于守方时，提高攻方固定敏，命中一定上升

当 `x > y` 时：

```text
∂hit/∂x = y * (x - 2y) / (2 * sqrt(0.02) * x^2 * sqrt(x - y))
```

它的符号取决于 `x - 2y`：

- `y < x < 2y` 时，`∂hit/∂x < 0`
- `x = 2y` 时，`∂hit/∂x = 0`
- `x > 2y` 时，`∂hit/∂x > 0`

这意味着：

- 攻方刚刚超过守方，但还没超过到 `2 倍` 时，继续加攻方固定敏，源码口径下命中率反而可能下降
- 到了极高敏差区，再继续加敏，命中率才重新转回上升

如果再把 `0.01%` 下限和 `75%` 上限算进去，则在被裁切的区间里，一阶导会直接变成 `0`。

所以严格说：

- “敏捷对命中一阶导恒非负”这个说法不成立
- 只能说它在某些区间非负，在某些区间为负


## 7. 会心公式

当前源码入口：`BATTLE_CriticalCheckPlayer()`。

### 7.1 基础输入

```text
At_Dex   = 攻方 CHAR_WORKFIXDEX
Df_Dex   = 守方 CHAR_WORKFIXDEX
At_Luck  = 攻方 CHAR_WORKFIXLUCK
At_Soubi = 武器 ITEM_CRITICAL
```

但这里同样要注意：

- 只有攻方是玩家时，源码才真的把 `At_Luck` 加进会心公式
- 宠物在主 PVP 会心分析里，可近似按 `At_Luck = 0` 看

### 7.2 身份修正

- 宠打敌：`Df_Dex = floor(Df_Dex * 0.8)`
- 敌打宠：`divpara = 10.0`，`root = 0`
- 非玩家打玩家：`divpara = 10.0`，`root = 0`
- 玩家打非玩家：`Df_Dex = floor(Df_Dex * 0.6)`

默认：

```text
divpara = 0.09
root = 1
```

### 7.3 核心公式

```text
若 At_Dex >= Df_Dex：
  Big = At_Dex
  Small = Df_Dex
  wari = 1.0
否则：
  Big = Df_Dex
  Small = At_Dex
  wari = Small / Big

Work = (Big - Small) / divpara
if Work <= 0 then Work = 0
```

如果 `root = 1`：

```text
per = (sqrt(Work) + At_Soubi * 0.5) * wari + At_Luck
```

如果 `root = 0`：

```text
per = (Work + At_Soubi * 0.5) * wari + At_Luck
```

最后：

```text
per *= 100
下限 = 1
上限 = 10000
若 RAND(1,10000) < per，则会心成立
```

### 7.4 玩家 PVP 最终公式速查

设：

- `x = 攻方固定敏`
- `y = 守方固定敏`
- `L = 攻方幸运`
- `C = 武器会心值`

则玩家 PVP 常用最终公式可整理成：

```text
若 x >= y：
  crit(x, y, L, C) = clip(sqrt((x - y) / 0.09) + L + 0.5 * C, 0.01, 100.00)
否则：
  crit(x, y, L, C) = clip((sqrt((y - x) / 0.09) + 0.5 * C) * (x / y) + L, 0.01, 100.00)
```

这里：

- 玩家 `L` 的有效范围只有 `1~5`
- 宠物在主 PVP 会心分析里通常可近似按 `L = 0`

### 7.5 一个很容易误会的源码点

会心成立以后，攻击流程里还会再看武器：

- 如果不是弓：走 `BATTLE_CriDamageCalc()`，吃会心伤害
- 如果是弓：仍然显示会心判定成立，但伤害走普通 `BATTLE_DamageCalc()`

也就是说：

- 弓不是“不会出会心”
- 而是“会心不吃额外伤害公式”

### 7.6 一个现实口径样例

```text
攻方固定敏 = 430
守方固定敏 = 400
攻方幸运   = 3
武器会心   = 10
```

则：

```text
Work = (430 - 400) / 0.09 = 333.33...
sqrt(Work) = 18.257...
per = 18.257 + 3 + 10 * 0.5 = 26.257...
```

最终会心率约：

```text
26.26%
```

如果攻方固定敏改成 `400`、守方 `430`，则会掉到约 `24.63%`。

### 7.7 敏捷对敏捷热力图，无 luck、无武器会心

这张图只看：

- 玩家对玩家
- 攻方幸运固定 `0`
- 武器会心固定 `0`
- 横轴、纵轴都用 `CHAR_WORKFIXDEX`

实际玩家图形可在此基础上整体上移 `1~5%`，再额外叠加 `0.5 * 武器会心值`。

![会心率基准图](battle_heatmaps/critical_pvp_luck0_wcrit0.svg)

### 7.8 优化会心的控制策略

如果你的目标是“追求高会心率”：

- 第一优先级仍然是固定敏领先。你一旦领先，`wari` 就回到 `1.0`。
- 每 `+1` 点幸运，理论上就是稳定 `+1%` 会心，但玩家幸运总共也只有 `1~5`，所以它更像保底修正。
- 每 `+1` 点武器会心，在固定敏领先时大约等于 `+0.5%` 会心；如果固定敏落后，还会再吃一次 `x / y` 折损。
- 所以“单点效率”上，幸运比武器会心更硬；但“总可堆空间”上，武器会心往往更大。
- 如果你玩的是弓，会心率本身仍然可判出，但不吃会心伤害增幅，所以堆会心的爆发收益会被打折。

### 7.9 敏捷对会心是否一阶导非负

答案同样是：

- 不是全局非负

先看最干净的玩家 PVP 基准式，并且先把幸运 `L`、武器会心 `C` 当成常数。

当 `x > y` 时：

```text
crit(x, y, L, C) = sqrt((x - y) / 0.09) + L + 0.5C
```

因此：

```text
∂crit/∂x = 1 / (2 * sqrt(0.09) * sqrt(x - y)) > 0
```

也就是：

- 攻方固定敏一旦超过守方，继续加敏，会心率单调上升

当 `x < y` 时，如果先看 `C = 0` 的基准式：

```text
crit(x, y, L, 0) = sqrt((y - x) / 0.09) * (x / y) + L
```

则：

```text
∂crit/∂x = (2y - 3x) / (2 * y * sqrt(0.09) * sqrt(y - x))
```

它的符号取决于 `2y - 3x`：

- `x < 2y/3` 时，`∂crit/∂x > 0`
- `x = 2y/3` 时，`∂crit/∂x = 0`
- `2y/3 < x < y` 时，`∂crit/∂x < 0`

也就是：

- 当攻方敏明显落后时，补敏会提高会心
- 但当攻方已经追到守方的约 `2/3` 以上、却还没追平时，再补一点攻方敏，会心率反而可能下降

如果把武器会心 `C` 也带进去，`x < y` 区间的一阶导会多一项正修正：

```text
∂crit/∂x
= (2y - 3x + 0.3C * sqrt(y - x))
  / (2 * y * sqrt(0.09) * sqrt(y - x))
```

所以：

- 武器会心会把“转负的拐点”往右推
- 但仍不能保证一阶导全程非负

再把 `0.01% ~ 100%` 的裁切加回去以后，被上限或下限压住的区间，一阶导同样会直接变成 `0`。


## 8. 反击公式与控制策略

当前源码入口：`BATTLE_Counter()`，实际概率判定在 `BATTLE_CounterCheckPlayer()` / `BATTLE_CounterCheckPet()`。

### 8.1 反击的硬门槛

至少要满足：

- 双方都活着
- 原始动作是 `普通攻击` 或 `舍身/无防御攻击` 那类可反击动作
- 攻方没有忠诚失控标记
- 双方都不是投掷武器反击禁用分支
- 通过 `BATTLE_CounterCheck()`

只要任意一边是远程：

- 弓
- 回力
- 投掷

对应反击分支就会直接失败。

### 8.2 核心敏捷公式

先算 `BATTLE_CounterCalc()`：

```text
At_Dex = 反击方 CHAR_WORKFIXDEX
Df_Dex = 被反击方 CHAR_WORKFIXDEX
```

默认：

```text
divpara = 0.08
root = 1
```

身份修正：

- 敌打宠：`divpara = 10`，`root = 0`
- 宠打敌：`Df_Dex = floor(Df_Dex * 0.8)`
- 非玩家打玩家：`divpara = 10`，`root = 0`
- 玩家打非玩家：`Df_Dex = floor(Df_Dex * 0.6)`

然后：

```text
若 At_Dex >= Df_Dex：
  Big = At_Dex
  Small = Df_Dex
  wari = 1.0
否则：
  Big = Df_Dex
  Small = At_Dex
  wari = Small / Big

Work = (Big - Small) / divpara
if Work <= 0 then Work = 0
```

- `root = 1` 时：`CriPer = sqrt(Work) * wari`
- `root = 0` 时：`CriPer = Work * wari`

### 8.3 玩家反击率

玩家分支会再乘武器对武器系数：

```text
per = CriPer * weapon_factor * 0.1 + At_Luck [+ CHAR_WORKCOUNTER]
```

这里的 `At_Luck` 同样只对玩家真正生效，玩家有效范围也是 `1~5`。

最后：

```text
per *= 100
若 RAND(1,10000) < per，则反击成功
```

### 8.4 一个样例

假设反击方是玩家：

```text
反击方固定敏 = 430
被反击方固定敏 = 400
反击方幸运   = 3
weapon_factor = 10
```

则：

```text
Work = (430 - 400) / 0.08 = 375
CriPer = sqrt(375) = 19.3649...
per = 19.3649 * 10 * 0.1 + 3 = 22.36%
```

### 8.5 反击的控制策略

- 要打反击，先保证自己和对面都不是远程武器。
- 固定敏领先，仍然是最稳的反击来源。
- 幸运对玩家反击是直接平加，但因为只有 `1~5`，它通常只是补强，不是主轴。
- 近战武器之间的 `weapon_factor` 差异是真实存在的，所以反击流不能只看面板敏，还要看武器对位。
- 当前主循环是 `for( k = 0; k < 5 && ContFlg == TRUE; k++ )`，所以最多会来回 `5` 段。

## 9. 法术、精灵命中公式

这里一定要先记住一条：

- 物理回避 / 会心看的是 `WORKFIXDEX`、`WORKFIXLUCK`
- 法术命中这里看的是 `CHAR_LUCK`，也就是原始幸运口径

而且：

- 玩家原始幸运是每日随机的 `1~5`
- 宠物不走这套每日幸运刷新

### 9.1 普通属性法术 / 精灵伤害命中

源码入口：`BATTLE_MagicDodge()`。

#### 目标是玩家

```text
miss_base = floor(CHAR_LUCK * 3
                + 对应属性抗性 * 0.15
                + 抗魔装备 * 0.9)
```

#### 目标不是玩家

```text
miss_base = floor(目标等级 * 0.2)
miss_base 最大 30
```

最后：

```text
若 rand()%100 + 1 > miss_base，则命中
否则 Miss
```

所以对玩家可直接理解成：

```text
命中率 = 100% - miss_base
```

这里还要特别说明一条很容易想错的结论：普通法术命中这一步
不看 HP。源码的 `BATTLE_MagicDodge()` 只读取：

- 目标原始幸运 `CHAR_LUCK`
- 目标对应属性抗性
- 抗魔装备修正
- 若目标不是玩家，则改读目标等级

它不读取下面这些量：

- 当前生命值 `CHAR_HP`
- 最大生命值 `CHAR_WORKMAXHP`
- 生命成长档或累计生命成长
- “不含装备修正的生命值”这一类派生口径

也就是说，普通法术命中里，生命高低本身没有影响；血量相关变量更多出现在伤害、治疗、吸血、百分比技能或特殊技能效果里，而不是这一步命中判定里。

### 9.2 职业法术命中

源码入口：`PROFESSION_MAGIC_DODGE()`。

有一条最前置的绝对规则：

- 如果目标正在 地球一周
- 直接 Miss

#### 目标是玩家

```text
miss_base = floor(CHAR_LUCK * 3
                + 对应职业属性抗性 * 0.5
                + 抗魔装备 * 0.4
                - 对应属性熟练 * 0.2)
```

#### 目标不是玩家

```text
miss_base = floor(目标等级 * 0.15 - 熟练 * 0.2)
等级项最高只算到 20
```

第一段命中：

```text
若 RAND(1,100) > miss_base，则通过第一段
否则 Miss
```

职业法术命中这里同样不看 HP。`PROFESSION_MAGIC_DODGE()` 实际读取的是：

- 目标原始幸运 `CHAR_LUCK`
- 目标对应职业属性抗性
- 抗魔装备修正
- 攻方对应属性熟练
- 若目标不是玩家，则改读目标等级近似项

它同样不读取：

- 当前生命值 `CHAR_HP`
- 最大生命值 `CHAR_WORKMAXHP`
- 生命成长档
- 不含装备修正的生命值

所以普通法术命中和职业法术命中，在“是否看生命值”这一点上结论一致：都不看 HP。

### 9.3 普通法术与职业法术命中对照

| 项目 | 普通属性法术 / 精灵 | 职业法术 |
| --- | --- | --- |
| 命中入口 | `BATTLE_MagicDodge()` | `PROFESSION_MAGIC_DODGE()` |
| 打玩家时读取 | 原始幸运、对应属性抗性、抗魔装备 | 原始幸运、对应职业属性抗性、抗魔装备、攻方熟练 |
| 打非玩家时读取 | 目标等级近似，最高 `30` | 目标等级近似，最高 `20`，再减熟练项 |
| 是否读取 HP | 不读取 | 不读取 |
| 是否有额外绝对 Miss | 无这一层前置 | 有，地球一周 时直接 Miss |
| 是否可能有第二段命中 | 没有 | 部分技能有 |

### 9.4 职业法术的第二段固定命中

部分技能在第一段通过后，还要再过一次固定命中：

- `电流术` / `暴风雨`：再判 `75%`
- `火龙枪` / `世界末日`：再判 `90%`
- `一针见血`：再判 `50%`

所以这些技能的最终命中率可以直接理解成：

```text
最终命中率 = 第一段命中率 * 第二段固定比例
```

### 9.5 冰爆术的特殊点

`冰爆术` 这段源码里原本写了一套命中分段，但后面又直接：

```c
hit = 100;
```

所以当前代码口径下，这一段额外命中视为：

- 必中

### 9.6 两个现实口径样例

#### 样例 1：普通属性法术打玩家

假设目标玩家：

```text
原始幸运 = 3
对应抗性 = 20
抗魔装备 = 10
```

则：

```text
miss_base = floor(3*3 + 20*0.15 + 10*0.9)
          = floor(9 + 3 + 9)
          = 21
```

所以普通属性法术命中率是：

```text
79%
```

#### 样例 2：职业法术打玩家

假设：

```text
目标原始幸运 = 3
目标对应抗性 = 20
目标抗魔装备 = 10
攻方对应熟练 = 60
```

则：

```text
miss_base = floor(3*3 + 20*0.5 + 10*0.4 - 60*0.2)
          = floor(9 + 10 + 4 - 12)
          = 11
```

第一段命中率：

```text
89%
```

于是：

- `电流术` / `暴风雨` 最终约 `66.75%`
- `火龙枪` / `世界末日` 最终约 `80.10%`
- `一针见血` 最终约 `44.50%`
- 没有第二段判定的职业法术，最终就是 `89%`

### 9.7 法术命中的控制策略

如果你的目标是“提高普通属性法术 / 精灵伤害命中”：

- 进攻方几乎没有可堆的命中属性，这一段主要看目标的原始幸运、对应抗性、抗魔装备。
- 但玩家原始幸运只有 `1~5`，所以普通法术真正拉开差距的，常常是抗性和抗魔装备，不是幸运本身。

对玩家目标来说，边际可以这样读：

- 目标 `+1` 原始幸运，命中约 `-3%`
- 目标 `+1` 对应抗性，命中约 `-0.15%`
- 目标 `+1` 抗魔装备，命中约 `-0.9%`

如果你的目标是“提高职业法术命中”：

- 进攻方真正能主动堆的核心项，是对应属性熟练。
- 玩家原始幸运仍然是最重的单项防御，但因为只有 `1~5`，它更多像底盘，不像无限堆高的面板项。

对职业法术打玩家来说，边际可以这样读：

- 目标 `+1` 原始幸运，第一段命中约 `-3%`
- 目标 `+1` 对应抗性，第一段命中约 `-0.5%`
- 目标 `+1` 抗魔装备，第一段命中约 `-0.4%`
- 攻方 `+5` 对应熟练，第一段命中约 `+1%`

### 9.7 命中和伤害是两层

法术命中以后，伤害还会继续被这几个东西影响：

```text
伤害 = power * (1 + proficiency/100)
             * (1 - resist/100)
             * (1 - suit/100)
             * (1 - spirit/100)
```

其中：

- `proficiency`：攻方对应熟练
- `resist`：守方对应抗性
- `suit`：守方对应抗性装备
- `spirit`：守方当前精灵抗性状态

所以“打不打得中”和“打中以后疼不疼”，在当前源码里是两套独立结算。

## 10. 属性场地机制

这部分是源码里真正的“属性场地 / 场地加强”，不是职业共通技能 `自我强化`。

### 10.1 场地设置

当前场地法术会写：

```text
BattleArray[battleindex].field_att = 场地属性
BattleArray[battleindex].att_pow = power
BattleArray[battleindex].att_count = turn
```

也就是：

- 场地属性
- 场地强度
- 持续回合

### 10.2 场地倍率公式

对某一单位，若场地属性与它某一属性对应，则：

```text
FieldPow = 0.5 + (该属性值 * att_pow * 0.01 * 0.01 * 0.5)
```

整理一下：

```text
FieldPow = 0.5 + 该属性值 * att_pow / 20000
```

因为属性值和 `att_pow` 都最多 100，所以：

```text
FieldPow 范围大约是 0.5 ~ 1.0
```

### 10.3 场地对最终伤害的影响

在属性相克伤害算完之后，再乘：

```text
damage = damage * (攻击方 FieldPow / 防守方 FieldPow)
```

所以：

- 攻击方更贴场地属性，伤害会上去
- 防守方更贴场地属性，伤害会被压下来

### 10.4 场地回合减少

每回合：

```text
att_count -= 1
att_count <= 0 时，field_att = NONE
```

## 11. 属性结界机制（四属性结界）

这一节说的是职业技能 `四属性结界`，不是 `火附体` / `冰附体` / `雷附体`。

### 11.1 结界存储方式

源码里用：

```c
MAKE2VALUE(power, turn)
```

写进：

- `CHAR_WORKFIXEARTHAT_BOUNDARY`
- `CHAR_WORKFIXWATERAT_BOUNDARY`
- `CHAR_WORKFIXFIREAT_BOUNDARY`
- `CHAR_WORKFIXWINDAT_BOUNDARY`

其中：

- 高 16 位存 `power`
- 低 16 位存 `turn`

### 11.2 持续回合

当前边界技能里写的是：

```text
skill_level >= 10 时：5 回合
skill_level > 8 时：3~4 回合（源码写法有一段重叠判断）
skill_level > 4 时：2 回合
否则：1 回合
```

由于源码这一段有重叠条件，边界值最好按“当前代码字面逻辑”理解，而不要按技能说明想当然推导。

### 11.3 结界强度

源码里按技能等级给 `power`：

```text
>=100 : 100
>95  : 90
>90  : 80
>85  : 70
>80  : 60
>60  : 50
>40  : 40
>20  : 30
否则  : 20
```

### 11.4 破结界

如果是“破结界”分支，会再判一次：

```text
skill_level >=100 : 100%
>80  : 80%
>40  : 70%
>20  : 60%
否则  : 50%
```

通过后：

```text
loop = 4
power = 0
turn = 0
```

也就是清掉该侧全部四种结界。

### 11.5 结界如何减伤

当前物理伤害结算里，防守方如果有某个属性结界，代码是：

- 土结界存在且攻方土属性 > 0：

```text
damage -= damage * (攻方土属性 / 200)
```

- 水结界存在且攻方水属性 > 0：

```text
damage -= damage * (攻方水属性 / 200)
```

- 火结界存在且攻方火属性 > 0：

```text
damage -= damage * (攻方火属性 / 200)
```

- 风结界存在且攻方风属性 > 0：

```text
damage -= damage * (攻方风属性 / 200)
```

### 11.6 一个非常关键的源码事实

当前这段减伤代码：

- 只检查“有没有该结界”
- 实际减伤量只看“攻击方对应属性值 / 200”
- **没有把结界的 `power` 真正乘进减伤公式里**

也就是说：

- 结界强度数值被存下来了
- 但当前减伤实现里基本只把它当“有/无”开关

这是当前源码里很值得注意的一个实现细节。

### 11.7 结界回合衰减

每回合会做：

```text
boundary_turn = low16(turn) - 1
turn < 0 时清空
否则写回 MAKE2VALUE(power, turn)
```

## 12. 三属性抗性机制

### 12.1 单属性抗性技能

- `火抗性`
- `冰抗性`
- `雷抗性`

当前加值都是：

```text
up_value = skill_level + 10
```

然后直接加到对应抗性：

```text
CHAR_WORK_F_RESIST / I_RESIST / T_RESIST += up_value
```

状态结束时再减回去。

### 12.2 自然守护 / 三抗

`自然威能` 的加值是分档的：

```text
10  -> 20
9   -> 18
8   -> 16
7   -> 14
6   -> 12
5   -> 10
4   -> 8
3   -> 6
2   -> 4
1   -> 2
```

然后同时加到：

- 火抗
- 冰抗
- 雷抗

## 13. 火 / 冰 / 雷三属性封印（火附体 / 冰附体 / 雷附体）

这一节说的是：

- `火附体`
- `冰附体`
- `雷附体`

不是上一节的 `四属性结界`。

### 13.1 主状态效果

#### 火封

回合结算时：

```text
DAMAGE = 50 * cnt
受到的最终伤害 = PROFESSION_MAGIC_GET_DAMAGE(自己, 自己, 火属性, DAMAGE)
```

也就是说：

- 火封是持续性掉血
- 掉血量会再过一次职业魔法伤害公式
- 仍然会吃熟练、抗性、抗装、精灵抗性这些修正

#### 冰封

回合结算时：

```text
old_dex = CHAR_DEX
WORKFIXDEX = floor(old_dex * 0.9)
```

这里取的是原始 `CHAR_DEX`，不是当前 `WORKFIXDEX`。

#### 雷封

当前代码里，雷封会直接让目标在可行动检查中失败：

```text
if CHAR_WORK_T_ENCLOSE > 0 -> 不能行动
```

### 13.2 附加异常触发

如果攻击方身上带有 `_ENCLOSE_2` 状态，并且这次打出了有效伤害，则会额外再做一次状态附加。

核心参数是：

```text
perStatus = 20 + skill_level * 2
```

持续回合：

```text
skill_level >= 10 -> 3 回合
skill_level >= 5  -> 2 回合
否则              -> 1 回合
```

其中雷系附加状态被强制写成：

```text
1 回合
```

## 14. `ABIO` 特殊敌方标记

当前源码里，这里讨论的是一个特殊战斗标记：

```text
CHAR_BATTLEFLG_ABIO
```

### 14.1 哪些单位会被打上这个标记

在战斗入场时，如果基础图号在：

```text
100466 ~ 100471
```

则会被打上 `ABIO` 标记。

### 14.2 这个标记会产生什么效果

#### 1. 不能闪避

在 `BATTLE_DuckCheck()` 最前面：

```text
defindex 若有 ABIO -> 直接 FALSE
```

也就是：

- 被攻击时不走普通闪避成功判定

#### 2. 不能主动进入反击链

在 `BATTLE_Counter()` 里：

```text
attackindex 若有 ABIO -> 直接 FALSE
```

#### 3. 被打死时，必定按 Ultimate 1 处理

在多处伤害收尾里，只要：

```text
目标 HP <= 0 且目标有 ABIO
```

就会：

```text
ultimate = 1
```

#### 4. 逃跑难度计算会特殊降低敌方平均等级

在逃跑判定里：

```text
若敌方有 ABIO，则 enemylevel -= 100
```

也就是说它会显著拉低“敌方平均等级”。

#### 5. 宠物逃走日志会走专门分支

源码里有 `EscapeLost` 的相关专门日志分支。

## 15. 其它值得单独点出来的机制

### 15.1 专注战斗

当前代码会直接写：

```text
CHAR_MYSKILLHIT = 2
CHAR_MYSKILLHIT_NUM = 100
```

也就是给命中相关判定一个很强的正修正。

### 15.2 挑拨

命中后会先写：

```text
rate = skill_level + 10
```

随后目标在状态生效时：

```text
STR  = STR  * (100 - rate) / 100
TGH  = TGH  * (100 - rate) / 100
DEX  = DEX  * (100 - rate) / 100
```

并且每回合还有：

```text
80% 概率强制转为普通攻击
```

### 15.3 树根缠绕

命中后：

```text
dex_down = skill_level * 4 + 选项基础值
WORKFIXDEX = WORKFIXDEX * (100 - dex_down) / 100
```

### 15.4 自我强化

这一点需要特别说明。

当前源码里：

```c
int PROFESSION_strengthen(...) {
    return TRUE;
}
```

也就是：

- `自我强化` 这个职业共通技能函数当前是空实现
- 源码中没有看到对应的战斗内数值公式

如果后续你要把它写进玩家文档，只能写成：

- “当前这份源码里没有实现具体数值效果”

而不能替它补一个想当然的公式。

### 15.5 骑宠时，攻击 / 防御 / 敏捷到底如何判定

当前文档统一按这组编译口径理解：

- `_BACK_VERSION` 未开启
- `_BATTLE_NEWPOWER` 已开启

源码入口：`BATTLE_adjustRidePet3A()`。

#### 15.5.1 先看“是否远程武器”

当前源码认定为远程的有：

- 弓
- 回力
- 投掷

只要命中 `BATTLE_IsThrowWepon()`，就走远程分支。

#### 15.5.2 骑宠攻击力

如果没骑宠：

```text
attack = 人物 WORKATTACKPOWER
```

如果有骑宠：

- 远程武器：

```text
attack = 人物攻击力 + 宠物攻击力 * 0.4
```

- 近战武器或空手：

```text
attack = 人物攻击力 * 0.8 + 宠物攻击力 * 0.8
```

注意：

- 这里不是平均
- 近战骑宠的攻击来源非常重，人物和宠物都各自吃了 `0.8`

#### 15.5.3 骑宠防御力

如果没骑宠，物理伤害函数会直接先取：

```text
defense = 人物 WORKDEFENCEPOWER * 0.70
```

如果有骑宠，先做骑宠混合：

```text
ride_def = 人物防御力 * 0.7 + 宠物防御力 * 0.7
```

然后伤害函数里再乘一次：

```text
defense = ride_def * 0.70
```

也就是在当前编译口径下：

```text
defense = 人物防御力 * 0.49 + 宠物防御力 * 0.49
```

#### 15.5.4 骑宠敏捷

行动顺序部分，前面第 4 节已经讲过。

但如果只看 `BATTLE_adjustRidePet3A()` 本体，骑宠状态下的敏捷读取是：

- 攻击侧 + 远程：

```text
quick = 人物 WORKQUICK * 0.8 + 宠物 WORKQUICK * 0.2
```

- 攻击侧 + 近战：

```text
quick = 人物 WORKQUICK * 0.2 + 宠物 WORKQUICK * 0.8
```

- 防守侧：

```text
quick = 人物 WORKQUICK * 0.1 + 宠物 WORKQUICK * 0.9
```

所以从源码直觉看：

- 骑宠近战明显更偏宠物
- 骑宠远程明显更偏人物
- 骑宠防守更几乎直接看宠物敏

### 15.6 物理最终伤害公式

源码入口：`BATTLE_DamageCalc()`。

这一节只整理当前 `_BATTLE_NEWPOWER` 分支。

#### 15.6.1 先取攻击与防御

```text
attack = 未骑宠 ? 人物攻击力 : 骑宠攻击混合值
defense = 未骑宠 ? 人物防御力 * 0.70 : 骑宠防御混合值 * 0.70
```

然后还可能继续吃这些修正：

- 铁壁防御：按状态百分比再增防
- 石化：`defense *= 2.0`
- 无装备防御类技能：直接把 `defense` 改成 `WORKFIXTOUGH`
- 忽视防御：按百分比继续扣掉 `defense`
- 怪物强化编译开关下，敌方攻防还会再吃一层小随机

#### 15.6.2 核心伤害分段

令：

- `A = attack`
- `D = defense`

则当前源码的基础物伤分三段：

若 `D > A`：

```text
damage = RAND(0, 1)
```

也就是几乎只能打出 `0` 或 `1`。

若 `D <= A < D * 8 / 7`：

```text
damage = RAND(0, A / 16)
```

若 `A >= D * 8 / 7`：

```text
K0 = RAND(0, A / 8) - A / 16
damage = (A - D) * 2.0 + K0
```

当前常量是：

```text
DAMAGE_RATE = 2.0
D_16 = 1/16
D_8  = 1/8
```

注意：

- `RAND()` 最终是整数随机
- `damage` 也是 `int`
- 所以这些浮点结果最后都会按截断落回整数

#### 15.6.3 属性、结界、额外伤害

核心物伤出来后，还会继续经过：

```text
damage = BATTLE_AttrAdjust(attackindex, defindex, damage)
```

也就是四属性相克、场地等口径。

然后如果守方开了 `四属性结界`，会再按攻方对应属性减伤：

```text
土结界: damage -= damage * (攻方土属性 / 200)
水结界: damage -= damage * (攻方水属性 / 200)
火结界: damage -= damage * (攻方火属性 / 200)
风结界: damage -= damage * (攻方风属性 / 200)
```

再之后，如果开了额外伤害 / 额外减伤编译：

```text
otherpower = RAND(apower * 0.3, apower) - RAND(dpower * 0.3, dpower)
damage += otherpower
damage < 0 时压回 0
```

#### 15.6.4 会心伤害

如果这次判出了会心，并且武器不是弓，则会走：

```text
cri_damage = normal_damage
           + 守方 WORKDEFENCEPOWER * 攻方等级 / 守方等级 * 0.5
```

也就是说：

- 会心不是把普通伤害直接乘倍数
- 而是在普通物伤结算完成后，再额外加一段和守方防御、双方等级有关的伤害

如果是弓：

- 仍然可以判出会心
- 但不会吃这段额外会心伤害


## 16. 一页版结论

如果你只想先记最重要的几条：

- 行动顺序主要看 `WORKQUICK`，但很多职业技能是否吃骑宠敏，取决于它是不是走默认速度档
- 法师大量技能走“只看人物敏”的专用速度档
- 白狼、猎人大多数战斗技能走默认档，所以骑宠与远程武器都会影响先手
- 普攻闪避要分“主动 `回避` 前判”和普通回避公式两层；普通回避公式本身上限 `75%`
- 会心看 `固定敏差 + 幸运 + 武器会心`，但弓即使判出会心，也不吃会心伤害增幅
- 反击不是只能 1 次，当前源码里最多能来回 `5` 段
- 普通法术命中主要看目标原始幸运、属性抗性、抗魔装备，进攻方几乎没有可堆命中项
- 职业法术命中还要额外看熟练度，且部分技能还有第二段固定命中判定
- 场地属性会按 `攻击方场地倍率 / 防守方场地倍率` 再乘一次
- `四属性结界` 当前减伤实现里，几乎只把结界当成“有无开关”，强度值并没有真正乘进减伤公式
- `自我强化` 当前源码是空实现
